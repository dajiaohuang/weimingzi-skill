[project]
name = "weimingzi"
version = "0.1.0"
description = "未明子视频 Whisper ASR 转写工具"
requires-python = ">=3.12"
dependencies = [
    "yt-dlp>=2026.6.9",
    "faster-whisper>=1.1.0",
]

[project.scripts]
whisper-transcribe = "whisper_transcribe:main"
fetch-videos = "fetch_bilibili_videos:main"
